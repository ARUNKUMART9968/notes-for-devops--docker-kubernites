# notes-for-devops--docker-kubernites
creating the docker img command
<img width="873" height="64" alt="image" src="https://github.com/user-attachments/assets/0450fb18-59c9-4550-9714-db6b524fd98b" />
above command is commen for all the language only changes in the names
A Docker image is a read-only blueprint (template) used to create containers.

👉 Think of it like:

📸 Image = Blueprint / Snapshot
📦 Container = Running instance of that image
If you build a Docker image with Java inside it, then:

👉 You DO NOT need Java installed on your system
👉 The app will run inside the container with its own Java environment
